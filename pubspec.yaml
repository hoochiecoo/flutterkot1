name: native_camera_app
description: Flutter WebView with Native Kotlin Camera and Permissions.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.7.0
  # ПЛАГИН ДЛЯ ЗАПРОСА ПРАВ (ОБЯЗАТЕЛЕН)
  permission_handler: ^11.3.0

flutter:
  uses-material-design: true
